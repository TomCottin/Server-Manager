spring:
    datasource:
        url: jdbc:postgresql://localhost:5432/server
        username: postgres
        password: root
    jpa:
        hibernate:
            ddl-auto: create
        properties:
           hibernate:
            dialect: org.hibernate.dialect.PostgreSQLDialect
            '[format_sql]': true
        show-sql: true
